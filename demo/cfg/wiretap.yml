version: 1
formatters:
  console_full:
    (): wiretap.modules.logging.TextFormatter
    style: "{"
    datefmt: "%Y-%m-%d %H:%M:%S"
    fmt: >-
      {asctime}.{msecs:03.0f} [{levelname:7s}]
      {indent}{operation}: "{message}", properties={properties}, span={span}, source={source}
  console_lite:
    (): wiretap.modules.logging.TextFormatter
    style: "{"
    datefmt: "%Y-%m-%d %H:%M:%S"
    fmt: >-
      [{asctime}.{msecs:03.0f} {levelname:7s}{indent}]
      {operation}: "{message}", properties={properties}
  elastic_json:
    (): wiretap.modules.logging.JsonFormatter
    encoders:
      - wiretap.modules.services.EncodeDateTime
      - wiretap.modules.services.EncodeUUID
      - wiretap.modules.services.EncodeSet
      - wiretap.modules.services.EncodePath
      - wiretap.modules.services.EncodeToDict
    properties:
      - wiretap.modules.services.AddTimestamp
      - wiretap.modules.services.AddMessage
      - wiretap.modules.services.AddProperties
      - wiretap.modules.services.AddSpan
      - wiretap.modules.services.AddSource
      - wiretap.modules.services.AddException
#      - (): wiretap.modules.logging.AddEnvironmentVariables
#        names:
#          - app_id
handlers:
  console_full:
    class: logging.StreamHandler
    formatter: console_full
    #level: INFO
  console_lite:
    class: logging.StreamHandler
    formatter: console_lite
    #level: INFO
  console_file:
    class: "logging.handlers.TimedRotatingFileHandler"
    when: "d"
    interval: 1
    filename: "c:\\temp\\wiretap_console.log"
    formatter: console_full
    #level: TRACE
  elastic_file:
    class: "logging.FileHandler"
    filename: "c:\\temp\\wiretap_elastic.log"
    formatter: elastic_json
    #level: DEBUG
loggers:
  root:
    #handlers: [ "console", "file", "elastic_file" ]
    handlers: [ "console_full", "console_file", "elastic_file" ]
    # handlers: [ "console_lite", "elastic_file" ]
    # handlers: [ "console" ]
    #level: "INFO"
    #level: "DEBUG"
    level: "TRACE"
  PIL:
    level: "WARNING"